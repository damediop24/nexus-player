fastapi==0.115.6
uvicorn[standard]==0.34.0
yt-dlp==2025.12.8
httpx==0.28.1
aiofiles==24.1.0
python-multipart==0.0.20
websockets==14.1
curl_cffi>=0.7.0
libtorrent==2.0.13

# Telegram restricted-content downloader bot (neex)
# kurigram is a maintained Pyrogram fork. TgCrypto is optional (speeds up crypto; requires MSVC++ Build Tools on Windows).
kurigram
pyleaves
python-dotenv
psutil
pillow
# TgCrypto  # uncomment after installing C++ Build Tools for faster MTProto if desired: pip install tgcrypto
